// Scoped to the `(list)` group so it never wraps `quotes/[id]` — see the
// boundary rule in `route-loading.tsx` and PROJECT-STRUCTURE.md §4.
export { RouteLoading as default } from "@/components/layout/route-loading";
