// `settings/` is a single page with no detail route, so the `(list)` group
// rule in PROJECT-STRUCTURE.md §4 does not apply and the bare form is correct.
export { RouteLoading as default } from "@/components/layout/route-loading";
